# tcpip

Working repository for [ゼロからのTCP/IPプロトコルスタック自作入門](https://book.mynavi.jp/ec/products/detail/id=149014).

## Usage

`terraform apply` at `/tf` dir, then created EC2 instance. SSH to the instance and run `./bin/setup` to setup Go development environment.
